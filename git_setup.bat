rem git+_setup.bat
rem
set GIT_USER=johndappsproton

rem Set the repository URL
set REPO_URL=https://github.com/johndappsproton/motherduck.git

rem Clone the repository (if it doesn't exist locally)
rem if not exist Motherduck git clone %REPO_URL%

rem Navigate to the repository directory
rem cd Motherduck

rem Add the files you want to upload
rem git add .

rem Commit the changes
rem git commit -m "Add/update files"
